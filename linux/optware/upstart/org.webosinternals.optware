description "Optware Bootstrap"

start on stopped finish

console output

script

echo -n "Mounting /opt directory: "
mkdir -p /var/opt || echo "Failed to create /var/opt" || return 1
mkdir -p /opt || echo "Failed to create /opt" || return 1
mount -o bind /var/opt /opt || echo "Failed to mount /opt" || return 1

end script
