This is the preware applications area.

Each subdirectory contains the packaging information for a single application package.

To create a new application package, do the following:

1) Create a new subdirectory using the short name of the application.

