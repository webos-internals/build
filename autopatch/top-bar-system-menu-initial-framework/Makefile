NAME = $(shell basename $(shell pwd))
PATCH = top-bar/${NAME}.patch
TITLE = System Menu Initial Framework
DESCRIPTION = Adds support for having two new system menus called status and today menus in addition to already existing application and device menus. Status menu is placed into left corner of top bar and the today menu in the center of top bar. Also adds support for menus to have editing option through gesture+tap.<br/><br/>NOTE: this patch itself does not add those new menus it only layouts the groundwork to allow other patches to create these menus.
CATEGORY = Top Bar
VERSIONS = 1.4.5-33
ICON = http://www.webos-internals.org/images/f/f9/Icon_WebOSInternals_Patch.png
SCREENSHOTS =
META_SUB_VERSION = 1

include ../common.mk

include ../modifications.mk

MAINTAINER = sconix
HOMEPAGE = http://forums.precentral.net/webos-patches/245440-patch-top-bar-system-menus-status-today-device-menus.html
