NAME = $(shell basename $(shell pwd))
PATCH = pdf-viewer/${NAME}.patch
TITLE = Hide Header
DESCRIPTION = This hides the header with the filename so the search function is usable. Compatible with the landscape patche for PDF Viewer; however, note that using search in landscape mode may cause your phone to crash.
CATEGORY = PDF Viewer
VERSIONS = 1.4.5-1
ICON = http://www.webos-internals.org/images/7/71/Icon_WebOSInternals_Patches_Pdfviewer.png
SCREENSHOTS =
META_SUB_VERSION = 1

include ../common.mk

include ../modifications.mk

MAINTAINER = tarirene
HOMEPAGE =
